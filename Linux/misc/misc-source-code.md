# Install from source code:

1. Retrieve source code:
 
`wget https://github.com/application.zip`

2. Unzip the zip

`unzip application.zip`

3. Change into the directory of unzipped directory

`cd application.zip`

4. Compile the code

`make`

5. Install the application:

`sudo make install`
