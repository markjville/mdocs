# Virsh commands:

To list all VMs, including those not runnng:

`virsh list --all `

To reboot a VM:

`virsh reboot (name-of-vm)`

To start a VM:

`virsh start (vm-name)`

To reboot a VM:

`virsh reboot (vm-name)`

To stop a VM:

`virsh stop (vm-name)`
