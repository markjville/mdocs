# Find examples

To search in current directory and subdirectories for a folder “assets”:

`find -name assets`

To search a specific location for all .jpg files

`find /home -name *.jpg`

Find an empty file within the current directory.

`Find . -type f -empty`
